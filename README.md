# dioxus-kit

App-agnostic utilities for Dioxus web apps.

## Usage

Add it as a git dependency, pinned to a tag:

```toml
[dependencies]
dioxus-kit = { git = "https://github.com/clemenscodes/dioxus-kit", tag = "v0.1.0" }
```
